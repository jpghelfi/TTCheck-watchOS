# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TTCheck-watchOS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire'

  # Pods for TTCheck-watchOS

  target 'TTCheck-watchOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TTCheck-watchOSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'TTCheck-watchOS WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TTCheck-watchOS WatchKit App

end

target 'TTCheck-watchOS WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TTCheck-watchOS WatchKit Extension

end
